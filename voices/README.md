# Local deployment notes

This directory is intentionally empty in Git. Put a private reference recording here on the
machine that owns the voice, and pass it to `examples/http_client.py` together with its exact
transcript. The repository `.gitignore` excludes audio files and model weights.
